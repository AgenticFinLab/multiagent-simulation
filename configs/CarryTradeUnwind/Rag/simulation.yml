# =============================================================================
# CarryTradeUnwind Rag Simulation Configuration
# =============================================================================

setting:
  name: "carrytrade_unwind_rag_simulation"
  description: "Carry trade unwind with RAG-augmented LLM agents"
  total_rounds: 200
  round_history_limit: 3
  record_path: "EXPERIMENT/CarryTradeUnwind/Rag/records"
  save_diagram_interval: 50

environment:
  dotenv_path: .env
  workspace: "."

ray:
  address: null
  namespace: "carrytrade_rag"
  actor_prefix: null
  num_cpus: null
  num_gpus: null
  object_store_memory: 536870912
  include_dashboard: false
  logging_level: "warning"
  log_to_driver: false
  actor_options:
    max_restarts: 3
    max_task_retries: 1
    max_concurrency: 10
  runtime_env:
    working_dir: null
    pip: []
    env_vars: {}
  serialization:
    use_arrow: true
    register_custom_serializers: true

players: !include players.yml
topology: !include topology.yml

communication:
  storage_path: "EXPERIMENT/CarryTradeUnwind/Rag/communication"
  record_messages: true
  message_block_size: 500
