# =============================================================================
# MomentumEffect Players Configuration
# =============================================================================
# All parameters explicitly defined - no defaults in code.
#
# Investor Types:
#   - MomentumTrader: Buys past winners, sells past losers (Jegadeesh & Titman 1993)
#   - ContrarianTrader: Mean reversion - buys losers, sells winners
#   - IndexFund: Passive baseline - fixed allocation
#   - MarketMaker: Liquidity provision - inventory mean reversion
#   - TechnicalTrader: Moving average crossover (golden/death cross)
#   - FundamentalTrader: Value-based anchor toward intrinsic value
# =============================================================================

market:
  name: "Market"
  class: "examples.MomentumEffect.players:Market"
  config:
    identity: "market"
    role: coordinator
    steps_per_turn: 1
    group_tags:
      - market
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Market parameters
      initial_price: 100.0
      initial_fundamental: 100.0
      price_impact: 0.08
      mean_reversion: 0.01         # Slow - allows momentum to persist
      noise_std: 0.3
      # Fundamental drift (creates momentum opportunities)
      drift_persistence: 0.95      # Autocorrelated drift
      drift_volatility: 0.5
      custom_state_hot_limit: 3
  persona: !include persona.yml

momentum_trader_1:
  name: "Momentum Trader 1"
  class: "examples.MomentumEffect.players:MomentumTrader"
  config:
    identity: "momentum_trader_1"
    role: player
    steps_per_turn: 1
    group_tags:
      - investors
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Base investor parameters
      initial_cash: 10000.0
      initial_position: 0.0
      custom_state_hot_limit: 3
      # MomentumTrader specific
      lookback_window: 5
      momentum_threshold: 0.02     # 2% threshold
      scale: 3.0                   # Aggressiveness
      max_position: 100.0
  persona: !include persona.yml

momentum_trader_2:
  name: "Momentum Trader 2"
  class: "examples.MomentumEffect.players:MomentumTrader"
  config:
    identity: "momentum_trader_2"
    role: player
    steps_per_turn: 1
    group_tags:
      - investors
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Base investor parameters
      initial_cash: 10000.0
      initial_position: 0.0
      custom_state_hot_limit: 3
      # MomentumTrader specific
      lookback_window: 5
      momentum_threshold: 0.02
      scale: 3.0
      max_position: 100.0
  persona: !include persona.yml

contrarian_trader:
  name: "Contrarian Trader"
  class: "examples.MomentumEffect.players:ContrarianTrader"
  config:
    identity: "contrarian_trader"
    role: player
    steps_per_turn: 1
    group_tags:
      - investors
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Base investor parameters
      initial_cash: 10000.0
      initial_position: 0.0
      custom_state_hot_limit: 3
      # ContrarianTrader specific
      reversion_threshold: 0.03    # 3% deviation
      scale: 2.0
      max_position: 80.0
  persona: !include persona.yml

index_fund:
  name: "Index Fund"
  class: "examples.MomentumEffect.players:IndexFund"
  config:
    identity: "index_fund"
    role: player
    steps_per_turn: 1
    group_tags:
      - investors
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Base investor parameters
      initial_cash: 10000.0
      initial_position: 0.0
      custom_state_hot_limit: 3
      # IndexFund specific
      target_allocation: 0.6       # 60% equity
      rebalance_threshold: 0.05    # 5% deviation triggers rebalance
  persona: !include persona.yml

market_maker:
  name: "Market Maker"
  class: "examples.MomentumEffect.players:MarketMaker"
  config:
    identity: "market_maker"
    role: player
    steps_per_turn: 1
    group_tags:
      - investors
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Base investor parameters
      initial_cash: 10000.0
      initial_position: 0.0
      custom_state_hot_limit: 3
      # MarketMaker specific
      inventory_target: 0.0
      reversion_speed: 0.2
  persona: !include persona.yml

technical_trader:
  name: "Technical Trader"
  class: "examples.MomentumEffect.players:TechnicalTrader"
  config:
    identity: "technical_trader"
    role: player
    steps_per_turn: 1
    group_tags:
      - investors
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Base investor parameters
      initial_cash: 10000.0
      initial_position: 20.0       # Start with some position
      custom_state_hot_limit: 3
      # TechnicalTrader specific (MA crossover)
      short_window: 3
      long_window: 10
      scale: 2.0
      max_position: 60.0
  persona: !include persona.yml

fundamental_trader:
  name: "Fundamental Trader"
  class: "examples.MomentumEffect.players:FundamentalTrader"
  config:
    identity: "fundamental_trader"
    role: player
    steps_per_turn: 1
    group_tags:
      - investors
    extras:
      record_path: "EXPERIMENT/MomentumEffect/records"
      # Base investor parameters
      initial_cash: 10000.0
      initial_position: 0.0
      custom_state_hot_limit: 3
      # FundamentalTrader specific
      value_threshold: 0.05        # 5% mispricing
      scale: 1.5
      max_position: 50.0
  persona: !include persona.yml
