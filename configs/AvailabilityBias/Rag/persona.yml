# =============================================================================
# AvailabilityBias Rag Persona Configuration
# =============================================================================

auto_checkpoint: false
debug_mode: true

proxy:
  storage:
    checkpoint_dir: "EXPERIMENT/AvailabilityBias/Rag/checkpoints"
    record_path: "EXPERIMENT/AvailabilityBias/Rag/records"
    record_rounds: true
    turn_block_size: 3

  monitoring:
    record_path: "EXPERIMENT/AvailabilityBias/Rag/monitoring"
    monitor_hot_limit: 3

  communication:
    message_timeout_ms: 5000
    enable_compression: true

  resource:
    mcp_servers: []
    enable_caching: false
