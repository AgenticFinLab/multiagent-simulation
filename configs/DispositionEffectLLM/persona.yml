# =============================================================================
# DispositionEffectLLM Persona Configuration
# =============================================================================

auto_checkpoint: false
debug_mode: true

proxy:
  storage:
    checkpoint_dir: "EXPERIMENT/DispositionEffectLLM/checkpoints"
    record_path: "EXPERIMENT/DispositionEffectLLM/records"
    record_rounds: true
  
  monitoring:
    record_path: "EXPERIMENT/DispositionEffectLLM/monitoring"
    entry_limit: 10
  
  communication:
    message_timeout_ms: 30000
    enable_compression: true
  
  resource:
    mcp_servers: []
    enable_caching: false
