knowledge:
  global_resources:
    MinerU_processed:
      path: "examples/document-sources/processed"
  global_index_dir:
    rag_index:
      path: "examples/document-sources/rag_index"

market:
  name: "Market"
  class: "examples.SVBBankRun.Rule.players:Market"
  num_instances: 1
  persona: !include ../Rag/persona.yml
  config:
    identity: "market"
    role: market
    steps_per_turn: 1
    group_tags: [market]
    extras:
      record_path: "EXPERIMENT/SVBBankRun/Rag/records/market"  # Source: variant runtime plumbing (RAG record path)
      initial_price: 100.0  # Source: target §9 market.extras.initial_price row; normalization (simulation-bases.md §6)
      fundamental_value: 100.0  # Source: target §9 market.extras.fundamental_value row; normalization; equal to initial_price so starting deviation is zero (simulation-bases.md §6)
      price_impact: 0.025  # Source: target §9 market.extras.price_impact row; Diamond and Dybvig (1983), 10.1086/261155
      mean_reversion: 0.01  # Source: target §9 market.extras.mean_reversion row; Bagehot (1873) via Freixas et al. (2000), 10.1023/A:1026527607455
      noise_std: 0.015  # Source: target §9 market.extras.noise_std row; Rule/players.py Market.perceive

ragllm_depositor:
  name: "Depositor"
  class: "examples.SVBBankRun.Rag.players:RagLLMDepositor"
  num_instances: 4  # Source: target §9 depositor.num_instances row
  persona: !include ../Rag/persona.yml
  config:
    identity: "ragllm_depositor"
    role: player
    steps_per_turn: 1
    group_tags: [investors]
    extras:
      agent_type: "depositor"  # Source: agent-design-skill §3.6.0 I/O Contract (LLM/RuleLLM/Rag variants)
      record_path: "EXPERIMENT/SVBBankRun/Rag/records/depositor"  # Source: variant runtime plumbing (RAG record path)
      initial_cash: 1000000.0  # Source: target §9 depositor.extras.initial_cash row
      initial_position: 600  # Source: target §9 depositor.extras.initial_position row
      initial_price: 100.0  # Source: target §9 market.extras.initial_price row (broadcast copy)
      fundamental_value: 100.0  # Source: target §9 market.extras.fundamental_value row (broadcast copy)
      withdrawal_threshold: 0.1  # Source: target §9 depositor.extras.withdrawal_threshold row; Diamond and Dybvig (1983), 10.1086/261155
      social_influence: 0.5  # Source: target §9 legacy descriptive extras (not consumed by current Rule formula)
      base_size: 300  # Source: target §9 legacy descriptive extras (not consumed by current Rule formula)
      custom_state_hot_limit: 3  # Source: target §9 custom_state_hot_limit row (runtime plumbing)
      private_knowledge:
        from_global_resources: ["MinerU_processed"]
        rag:
          from_global_index_dir: ["rag_index"]
          embed_type: "litellm"
          embed_model: "openai/hunyuan-embedding"
          embed_api_base: "https://api.hunyuan.cloud.tencent.com/v1"
          embed_api_key: "{{ HUNYUAN_API_KEY }}"
          chunk_size: 512
          chunk_overlap: 64
          top_k: 3
      llm:
        sys_message: "examples.SVBBankRun.Rag.prompts:RAGLLM_DEPOSITOR_SYS"
        user_message: "examples.SVBBankRun.Rag.prompts:RAG_USER_TEMPLATE"
        lm_name: "ark/doubao-seed-2-0-mini-260428"
        generation_config:
          temperature: 0.7
          max_tokens: 600

ragllm_social_media_influencer:
  name: "SocialMediaInfluencer"
  class: "examples.SVBBankRun.Rag.players:RagLLMSocialMediaInfluencer"
  num_instances: 2  # Source: target §9 socialmediainfluencer.num_instances row
  persona: !include ../Rag/persona.yml
  config:
    identity: "ragllm_social_media_influencer"
    role: player
    steps_per_turn: 1
    group_tags: [investors]
    extras:
      agent_type: "socialmediainfluencer"  # Source: agent-design-skill §3.6.0 I/O Contract (LLM/RuleLLM/Rag variants)
      record_path: "EXPERIMENT/SVBBankRun/Rag/records/socialmediainfluencer"  # Source: variant runtime plumbing (RAG record path)
      initial_cash: 800000.0  # Source: target §9 socialmediainfluencer.extras.initial_cash row
      initial_position: 500  # Source: target §9 socialmediainfluencer.extras.initial_position row
      initial_price: 100.0  # Source: target §9 market.extras.initial_price row (broadcast copy)
      fundamental_value: 100.0  # Source: target §9 market.extras.fundamental_value row (broadcast copy)
      amplification_factor: 2.0  # Source: target §9 socialmediainfluencer.extras.amplification_factor row; Bikhchandani et al. (1992), 10.1086/261849
      base_size: 500  # Source: target §9 legacy descriptive extras (not consumed by current Rule formula)
      custom_state_hot_limit: 3  # Source: target §9 custom_state_hot_limit row (runtime plumbing)
      private_knowledge:
        from_global_resources: ["MinerU_processed"]
        rag:
          from_global_index_dir: ["rag_index"]
          embed_type: "litellm"
          embed_model: "openai/hunyuan-embedding"
          embed_api_base: "https://api.hunyuan.cloud.tencent.com/v1"
          embed_api_key: "{{ HUNYUAN_API_KEY }}"
          chunk_size: 512
          chunk_overlap: 64
          top_k: 3
      llm:
        sys_message: "examples.SVBBankRun.Rag.prompts:RAGLLM_SOCIAL_MEDIA_INFLUENCER_SYS"
        user_message: "examples.SVBBankRun.Rag.prompts:RAG_USER_TEMPLATE"
        lm_name: "ark/doubao-seed-2-0-mini-260428"
        generation_config:
          temperature: 0.7
          max_tokens: 600

ragllm_bank_manager:
  name: "BankManager"
  class: "examples.SVBBankRun.Rag.players:RagLLMBankManager"
  num_instances: 2  # Source: target §9 bankmanager.num_instances row
  persona: !include ../Rag/persona.yml
  config:
    identity: "ragllm_bank_manager"
    role: player
    steps_per_turn: 1
    group_tags: [investors]
    extras:
      agent_type: "bankmanager"  # Source: agent-design-skill §3.6.0 I/O Contract (LLM/RuleLLM/Rag variants)
      record_path: "EXPERIMENT/SVBBankRun/Rag/records/bankmanager"  # Source: variant runtime plumbing (RAG record path)
      initial_cash: 3000000.0  # Source: target §9 bankmanager.extras.initial_cash row
      initial_position: 800  # Source: target §9 bankmanager.extras.initial_position row
      initial_price: 100.0  # Source: target §9 market.extras.initial_price row (broadcast copy)
      fundamental_value: 100.0  # Source: target §9 market.extras.fundamental_value row (broadcast copy)
      duration_gap: 0.5  # Source: target §9 bankmanager.extras.duration_gap row (informational; not consumed in Rule)
      stabilize_size: 400  # Source: target §9 legacy descriptive extras (not consumed by current Rule formula)
      custom_state_hot_limit: 3  # Source: target §9 custom_state_hot_limit row (runtime plumbing)
      private_knowledge:
        from_global_resources: ["MinerU_processed"]
        rag:
          from_global_index_dir: ["rag_index"]
          embed_type: "litellm"
          embed_model: "openai/hunyuan-embedding"
          embed_api_base: "https://api.hunyuan.cloud.tencent.com/v1"
          embed_api_key: "{{ HUNYUAN_API_KEY }}"
          chunk_size: 512
          chunk_overlap: 64
          top_k: 3
      llm:
        sys_message: "examples.SVBBankRun.Rag.prompts:RAGLLM_BANK_MANAGER_SYS"
        user_message: "examples.SVBBankRun.Rag.prompts:RAG_USER_TEMPLATE"
        lm_name: "ark/doubao-seed-2-0-mini-260428"
        generation_config:
          temperature: 0.7
          max_tokens: 600

ragllm_regulator:
  name: "Regulator"
  class: "examples.SVBBankRun.Rag.players:RagLLMRegulator"
  num_instances: 1  # Source: target §9 regulator.num_instances row
  persona: !include ../Rag/persona.yml
  config:
    identity: "ragllm_regulator"
    role: player
    steps_per_turn: 1
    group_tags: [investors]
    extras:
      agent_type: "regulator"  # Source: agent-design-skill §3.6.0 I/O Contract (LLM/RuleLLM/Rag variants)
      record_path: "EXPERIMENT/SVBBankRun/Rag/records/regulator"  # Source: variant runtime plumbing (RAG record path)
      initial_cash: 10000000.0  # Source: target §9 regulator.extras.initial_cash row
      initial_position: 0  # Source: target §9 regulator.extras.initial_position row
      initial_price: 100.0  # Source: target §9 market.extras.initial_price row (broadcast copy)
      fundamental_value: 100.0  # Source: target §9 market.extras.fundamental_value row (broadcast copy)
      intervention_threshold: 0.5  # Source: target §9 regulator.extras.intervention_threshold row; Bagehot (1873)
      guarantee_probability: 0.4  # Source: target §9 regulator.extras.guarantee_probability row; Freixas et al. (2000), 10.1023/A:1026527607455
      intervention_size: 2000  # Source: target §9 regulator.extras.intervention_size row (config extra; runtime code uses literal 2000)
      custom_state_hot_limit: 3  # Source: target §9 custom_state_hot_limit row (runtime plumbing)
      private_knowledge:
        from_global_resources: ["MinerU_processed"]
        rag:
          from_global_index_dir: ["rag_index"]
          embed_type: "litellm"
          embed_model: "openai/hunyuan-embedding"
          embed_api_base: "https://api.hunyuan.cloud.tencent.com/v1"
          embed_api_key: "{{ HUNYUAN_API_KEY }}"
          chunk_size: 512
          chunk_overlap: 64
          top_k: 3
      llm:
        sys_message: "examples.SVBBankRun.Rag.prompts:RAGLLM_REGULATOR_SYS"
        user_message: "examples.SVBBankRun.Rag.prompts:RAG_USER_TEMPLATE"
        lm_name: "ark/doubao-seed-2-0-mini-260428"
        generation_config:
          temperature: 0.7
          max_tokens: 600

ragllm_bond_trader:
  name: "BondTrader"
  class: "examples.SVBBankRun.Rag.players:RagLLMBondTrader"
  num_instances: 2  # Source: target §9 bondtrader.num_instances row
  persona: !include ../Rag/persona.yml
  config:
    identity: "ragllm_bond_trader"
    role: player
    steps_per_turn: 1
    group_tags: [investors]
    extras:
      agent_type: "bondtrader"  # Source: agent-design-skill §3.6.0 I/O Contract (LLM/RuleLLM/Rag variants)
      record_path: "EXPERIMENT/SVBBankRun/Rag/records/bondtrader"  # Source: variant runtime plumbing (RAG record path)
      initial_cash: 2000000.0  # Source: target §9 bondtrader.extras.initial_cash row
      initial_position: 400  # Source: target §9 bondtrader.extras.initial_position row
      initial_price: 100.0  # Source: target §9 market.extras.initial_price row (broadcast copy)
      fundamental_value: 100.0  # Source: target §9 market.extras.fundamental_value row (broadcast copy)
      yield_sensitivity: 0.5  # Source: target §9 legacy descriptive extras (not consumed by current Rule formula)
      position_size: 350  # Source: target §9 legacy descriptive extras (not consumed by current Rule formula)
      custom_state_hot_limit: 3  # Source: target §9 custom_state_hot_limit row (runtime plumbing)
      private_knowledge:
        from_global_resources: ["MinerU_processed"]
        rag:
          from_global_index_dir: ["rag_index"]
          embed_type: "litellm"
          embed_model: "openai/hunyuan-embedding"
          embed_api_base: "https://api.hunyuan.cloud.tencent.com/v1"
          embed_api_key: "{{ HUNYUAN_API_KEY }}"
          chunk_size: 512
          chunk_overlap: 64
          top_k: 3
      llm:
        sys_message: "examples.SVBBankRun.Rag.prompts:RAGLLM_BOND_TRADER_SYS"
        user_message: "examples.SVBBankRun.Rag.prompts:RAG_USER_TEMPLATE"
        lm_name: "ark/doubao-seed-2-0-mini-260428"
        generation_config:
          temperature: 0.7
          max_tokens: 600
