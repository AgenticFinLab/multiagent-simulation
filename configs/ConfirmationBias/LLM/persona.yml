# =============================================================================
# ConfirmationBias LLM Persona Configuration
# =============================================================================

auto_checkpoint: false
debug_mode: true

proxy:
  storage:
    checkpoint_dir: "EXPERIMENT/ConfirmationBias/LLM/checkpoints"
    record_path: "EXPERIMENT/ConfirmationBias/LLM/records"
    record_rounds: true
    turn_block_size: 3

  monitoring:
    record_path: "EXPERIMENT/ConfirmationBias/LLM/monitoring"
    monitor_hot_limit: 3

  communication:
    message_timeout_ms: 30000
    enable_compression: true

  resource:
    mcp_servers: []
    enable_caching: false
