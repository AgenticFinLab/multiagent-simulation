# GFC2008 LLM Persona Configuration

auto_checkpoint: false
debug_mode: true

proxy:
  storage:
    checkpoint_dir: "EXPERIMENT/GFC2008/LLM/checkpoints"
    record_path: "EXPERIMENT/GFC2008/LLM/records"
    record_rounds: true
    turn_block_size: 3

  monitoring:
    record_path: "EXPERIMENT/GFC2008/LLM/monitoring"
    monitor_hot_limit: 3

  communication:
    message_timeout_ms: 5000
    enable_compression: true

  resource:
    mcp_servers: []
    enable_caching: false
