# =============================================================================
# Persona Configuration
# =============================================================================
# Defines proxy settings and runtime behavior for a player's persona.
# Each player can have its own persona config, or share one via !include.
# =============================================================================

auto_checkpoint: false
debug_mode: true

proxy:
  storage:
    checkpoint_dir: "EXPERIMENT/Demo/checkpoints"
    record_path: "EXPERIMENT/Demo/records"
    record_rounds: true
    turn_block_size: 3
  
  monitoring:
    record_path: "EXPERIMENT/Demo/monitoring"
    monitor_hot_limit: 3
  
  communication:
    message_timeout_ms: 5000
    enable_compression: true
  
  resource:
    mcp_servers: []
    enable_caching: false
