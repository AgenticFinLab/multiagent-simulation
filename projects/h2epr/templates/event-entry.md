# `<Event title>`

State the benchmark event ID, stable slug, selected process window, Source
Profile exposure, and the bounded simulation objective in one compact opening
paragraph. Remove these instructions from the published entry.

## Event assets

Link the current Source Profile, roster/actor map, Agent Definitions and
Population Models, participant interface release, Scenario release, shared and
backend configurations, Backend Realization, assembly, and compiled package.
Each role has one current path. Name any dataset defect or recorded
representation limitation that affects how the asset tree should be read.

## Backend status

| Backend | Status | Current evidence or failure |
|---|---|---|
| Rule | implemented / unavailable | binding, realization, configuration, and release links |
| LLM | planned / implemented / unavailable | exact reason and required successor |
| RuleLLM | planned / implemented / unavailable | exact reason and required successor |

Do not describe a catalog row as implemented until package admission can load
its exact registered factory and binding.

## Current result

Link the compact run release and simulation-only reading. Record package, run,
trace, terminal-state, replay, and Generated EPG identities; actor, tick,
record, node, edge, and unresolved-transport counts; deterministic/model
evidence; current registry status; and cross-event status (`not applicable`
for fewer than two current events).

State the exact validation commands and whether raw custody remains available.
The entry reports the accepted endpoint, not the construction diary.

## Claim boundary

State what the current result supports: dataset-conditioned package,
execution, integrity, replay, graph provenance, and bounded simulation reading
as applicable. Explicitly exclude historical fit, parameter calibration,
held-out performance, policy effects, causality, scientific validity, and
universal generality unless a separate accepted protocol establishes them.

Name the next legal action without presenting it as already authorized.
