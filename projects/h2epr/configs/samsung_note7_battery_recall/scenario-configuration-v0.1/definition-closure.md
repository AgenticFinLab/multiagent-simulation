# H2EPR-0481 Scenario Configuration definition closure

## 1. Closure scope

This record checks that configuration
`h2epr.0481.scenario.mechanism-coverage.v0_1@0.1.0` fixes every
configuration-owned choice without changing the accepted Scenario, mapping,
Roster, or evidence. Closure is semantic completeness for a non-executable
configuration, not runtime or scientific validation.

## 2. Input and format closure

The machine document pins the Scenario release and Definition, interface
closure, roster release, consolidated mapping and mapping profile, semantic
skeleton, and both evidence authorities by exact SHA-256. It declares the
closed semantic format and uses its domain-neutral structural vocabulary.

The v0.2 schema is additive at the loader boundary: the frozen v0.1 schema and
accepted SingHealth configuration continue to validate through their original
identity and vocabulary. A new `semantic_kind` property records whether a
canonical registry entry is a legal institution or a scoped market, consumer,
or operator resource domain.

## 3. Configuration-owned choice closure

| Family | Machine carrier | Result |
|---|---|---|
| purpose and claims | top-level purpose and false historical flags | closed |
| clock and order | `clock` | closed |
| structural baseline | six variants plus exact materializations | closed |
| canonical registries and resources | seven typed registry entries and eight resource objects | closed |
| participant assembly | four named actors and four Population units | closed |
| opening state | 34 based records | closed |
| routes | eight exact-addressed records | closed |
| exogenous input | six typed non-outcome-forcing inputs | closed |
| policy meaning | nine unbound fail-closed selections | closed |
| sensitivity | six paired exact overlays | closed |
| bounded lineage | four participants, three routes, seven intents | closed |
| completion | normal, bounded-incomplete, and invariant-failure rules | closed |

## 4. Assembly and reference closure

Every entity, actor, unit, capacity, authority graph, resource owner, asset,
record, route, process, input, policy, variant, overlay, and lineage identity
is unique within its class. The four named actors exactly equal the Agent
Definition product set. The four units exactly equal the Population Model
product set and each has one actor, host/resource domain, capacity, assignment,
availability record, composition, access scope, and private-state default.

All route endpoints resolve to exact actors. Multi-endpoint routes require one
exact sender and one exact recipient and forbid set broadcast. The bounded
lineage resolves pairwise through Samsung--regional, regional--outlet, and
outlet--consumer routes; its intent sequence contains every lineage capability
in causal order, including consumer remedy request before outlet response.

## 5. Opening-state closure

| Record family | Count | Closure condition |
|---|---:|---|
| authority/capacity | 4 | exact actor, capacities, owner, interval, availability, and basis |
| unit assignment | 4 | exact unit, capacity, host, interval, access scope, and basis |
| relationship | 4 | canonical parties and scoped meaning |
| route | 8 | exact endpoints, addressing, ownership, visibility, and basis |
| resource state | 8 | stable identity with unsupported instances and prestates unknown |
| business-process state | 6 | no future incident, stop, recall, remedy, order, or encounter result preloaded |

All intervals fall within the configuration clock. No record imports January
2017 diagnosis or another known future result.

## 6. Variant, input, policy, and overlay closure

Every structural selection belongs to its allowed domain and points to exactly
one overlay. Each overlay has one exact selection replacement and one matching
materialization replacement of the same JSON value type. Population and
authority alternatives identify the exact actors affected.

All six exogenous inputs resolve to declared processes, resources, authority
records, or routes; each is non-outcome-forcing. All nine policy selections
remain unbound and fail closed. Execution eligibility is false in both the
document and its validation expectations.

## 7. Inventory closure

| Expected surface | Reconciled |
|---|---:|
| semantic products | 8 |
| decisions and population situations | 22 |
| observation placements | 40 |
| private-state placements | 28 |
| intent placements | 37 |
| lifecycle families | 12 |
| total actors / units | 8 / 4 |
| resources / registry entries | 8 / 7 |
| opening records / routes | 34 / 8 |
| variants / inputs / policies / overlays | 6 / 6 / 9 / 6 |

The mapping release supplies participant placement counts; the machine graph
supplies assembly counts. Neither establishes behavior or historical fit.

## 8. Residual boundary

No unresolved configuration-to-Definition semantic gap remains. Later work
owns static admission, event-qualified carrier projection, policy
implementation, participant binding, runtime identity, trace closure,
deterministic replay, execution, publication, and three-event conformance.

Any mismatch must return to its actual owner rather than be repaired by a
hidden default or outcome-conditioned change.
