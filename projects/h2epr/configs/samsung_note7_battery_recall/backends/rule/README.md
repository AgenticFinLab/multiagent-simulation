# Samsung Galaxy Note7 Battery Recall — Rule configuration

## Authority

The configuration contains only deterministic decision-production settings. Every top-level setting has explicit provenance, no model or network access is permitted, and unmatched coordinates use typed `no_op`.

## Evidence and failure routing

`manifest.json` pins every artifact, semantic parent, role, size, and content
hash. `SHA256SUMS` is the exact local publication surface. Schema, identity,
parent, path, coverage, or checksum drift rejects admission; correction
requires another successor.

## Claim boundary

Full Draft exposure supports dataset-conditioned engineering and method
verification only. This release establishes no held-out result, historical
fit, calibration, causal claim, scientific validity, or universal generality.
