# Lebanese Civil War Rule configuration

Thirty-three guarded rows select bounded coalition, intervention, camp,
mediation, faction-position and post-war records. Inclusive windows permit
waiting for actual information; accepted rows finish once and unchanged
rejection cannot create a retry. `no_op` is the explicit fallback.

All six top-level settings have provenance. Rule cannot decide combat,
casualties, territory, responsibility, agreement implementation or peace.
