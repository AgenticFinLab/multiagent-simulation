# East Palestine Train Derailment Rule configuration

Fourteen deterministic windowed rows select the current baseline within the
declared participant choice surface. Lower numeric priorities decide between
simultaneously eligible rows; an accepted row completes once. `no_op` remains a
valid wait when state or retained-message guards do not hold. Each row ends by
c10, leaving c11 for terminal delivery accounting.

The earliest windows preserve exposed interval boundaries. Dependencies such as
NTSB notice before DOJ filing and resident report before EPA oversight are
authored assumptions, not reconstructed decision protocols. Configuration uses
no model or network access and cannot alter the shared world. Admission and
provenance coverage are independently recomputed; outcome expectations do not
gate release validity.
