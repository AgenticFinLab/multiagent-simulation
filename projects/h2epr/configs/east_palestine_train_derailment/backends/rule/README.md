# East Palestine Train Derailment Rule configuration

This backend-only configuration contains fourteen deterministic rows and the explicit `no_op` fallback. It exercises every non-default intent once, uses no model or network access, and cannot alter the shared package. The receipt and provenance coverage are independently recomputable.
