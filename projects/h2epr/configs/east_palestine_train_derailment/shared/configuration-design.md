# East Palestine Train Derailment shared configuration

## Identity and purpose

Configuration `h2epr.0196.comparison.v1` selects the sole current shared-world values for `H2EPR-0196`. It is full-Draft-exposed and supports deterministic engineering and descriptive simulation only.

## Exact inputs

The configuration binds roster `h2epr.0196.roster.v1`, actor map `h2epr.0196.actor-map.v1`, participant interface `h2epr.0196.participant-interface.v1`, Scenario Interface `h2epr.0196.scenario-interface.v1`, Mechanism `h2epr.0196.scenario-mechanism.v1`, and declarative environment/annotation implementations. The compiler independently rederives the admission receipt from those bytes and the sealed Draft.

## Shared comparison settings

Eleven coordinates preserve all eight Draft episodes plus a delivery barrier. Seven active actors, eleven opening fields, eleven directed routes, sealed observations, mandatory coordinate completion, and zero unresolved transport are explicit. Calendar compression and aggregation are recorded assumptions. Every top-level setting has one provenance pointer and no exemption.

## Validation and promotion

Schema and self-hash validity are insufficient alone. Promotion requires Draft roster equality, semantic-parent anchors, actor/intent/state/route closure, independently rederived receipt equality, fresh package compilation, runtime replay, and publication verification. Any changed setting requires a new identity and rerun.
