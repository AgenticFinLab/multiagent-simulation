# East Palestine Train Derailment shared configuration

## Identity and purpose

Configuration `h2epr.0196.comparison.v2` selects the sole current shared-world values for `H2EPR-0196`. It is full-Draft-exposed and supports deterministic engineering and descriptive simulation only.

## Exact inputs

The configuration binds roster `h2epr.0196.roster.v1`, actor map `h2epr.0196.actor-map.v2`, participant interface `h2epr.0196.participant-interface.v2`, Scenario Interface `h2epr.0196.scenario-interface.v2`, Mechanism `h2epr.0196.scenario-mechanism.v2`, and declarative environment/annotation implementations. The compiler independently rederives the admission receipt from those bytes and the sealed Draft.

## Shared comparison settings

Eleven coordinates preserve all eight Draft episodes plus a delivery barrier. Seven active actors, twelve opening fields, eleven directed routes, sealed observations, mandatory coordinate completion, and zero unresolved transport are explicit. Calendar compression and aggregation are recorded assumptions. Every top-level setting has one provenance pointer and no exemption.

## Validation and promotion

Schema and self-hash validity are insufficient alone. Promotion requires Draft roster equality, semantic-parent anchors, actor/intent/state/route closure, independently rederived receipt equality, fresh package compilation, runtime replay, and publication verification. Any changed setting requires a new identity and rerun.

## Behavior and closure choices

The derailment is opening context; the operator notification starts unreported.
Controlled-release state records an instruction only. All routes select one tick;
received-message memory lets EPA combine differently timed reports and lets DOJ
retain a notice until the legal window. These are structural assumptions, not
calibrated latency or reconstructed internal institutional knowledge.

All eleven opportunities execute. The selected Rule windows end by c10 so the
last opportunity can account for delivery. Descriptive outcome expectations are
reported separately from trace, seal, replay, graph, and transport integrity.
An absent legal filing or settlement is a legitimate terminal record. A failed
attempt preserves its diagnostic prefix and cannot be published as a complete run.
