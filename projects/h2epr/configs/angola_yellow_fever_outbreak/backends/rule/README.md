# Angola Yellow Fever Outbreak of 2016 Rule configuration

This backend-only configuration contains 26 deterministic rows and the explicit `no_op` fallback. It exercises every non-default intent once, uses no model or network access, and cannot alter shared package semantics. Admission and provenance evidence are independently recomputable.
