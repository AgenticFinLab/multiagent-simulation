# Panic of 1907

H2EPR-0288 uses the stable slug `panic_of_1907`. Its full-Draft, dataset-only Rule
baseline models a bounded institutional record path from the failed United
Copper scheme through withdrawal pressure, aid decisions, trust suspension,
liquidity interventions, convertibility suspension and coarse post-crisis reform.

## Event assets

The [Source Profile](source-profile.json) seals exactly `event_spec.json`,
`frozen_evidence.json` and `draft_epg.json`. The
[roster and actor map](../../agents/rosters/panic_of_1907/) account for all 16 source
IDs and 33 appearances while recording the absent P_8 sequence value. Eight
[Agent Definitions](../../agents/defines/panic_of_1907/) and three
[Population Models](../../populations/models/panic_of_1907/) own human semantics; five
other source participants remain explicit context. The
[participant interface](../../agents/interfaces/panic_of_1907/) projects four
observation classes, 23 event intents plus `no_op`, and two lifecycles.

The [Scenario](../../scenarios/panic_of_1907/) defines the sparse record world,
authority and reducer. It rejects malformed Draft relationship/transaction
endpoints as authority and separates aggregate records from individual behavior.
The [shared configuration](../../configs/panic_of_1907/shared/) owns 20 coordinates,
opening state and routes; the
[Rule configuration](../../configs/panic_of_1907/backends/rule/) owns selected choices.
The [Rule realization](../../execution/panic_of_1907/rule/) pins the common
implementation. [Assembly](package-assembly.json) compiles to the backend-neutral
[event package](package/).

Prices, rates, balances, transfers, solvency, system recovery, votes and policy
effectiveness remain outside the reducer. The 1907 acute phase and 1913 statute
are connected only through disclosed logical dependencies, not calibrated time
or a historical causal estimate.

## Backend status

| Backend | Status | Current evidence or failure |
|---|---|---|
| Rule | implemented | admitted configuration/realization, exact package binding and independently verified run release |
| LLM | planned | no registered model-decision implementation or provenance; admission fails closed |
| RuleLLM | planned | no standardized proposal/admission implementation; admission fails closed |

Rule attaches without changing the backend-neutral package core. Planned catalog
entries provide no execution authority or fallback.

## Current result

The canonical seed-0 run is `run.1b16d1949b2a609181e1d06d`: eleven actors, 20 coordinates,
1,043 trace records,
1,084 graph nodes and
3,111 edges. Fresh A/B, generated-ID perturbation,
trace and tick/run seals, authoritative replay, complete graph reconstruction and
zero unresolved transport are independently verified in the
[run release](../../releases/panic_of_1907/rule/).

The [simulation reading](../../reports/panic_of_1907/rule/simulation-reading.md)
accounts for every output family, coordinate, non-default action and expectation.
A missing-aid-request probe and delayed-recommendations probe demonstrate valid
open endpoints under changed information while preserving execution integrity.
Raw custody is retained below ignored `.local-runtime/h2epr-simulation/`.

The [current registry](../current-events.json) and
[cross-event Rule conformance](../../releases/cross-event/rule/) cover the
accepted event set. Descriptive endpoint agreement is reported separately from
publication integrity.

## Claim boundary

This event establishes dataset-conditioned semantic construction,
state/information-aware deterministic Rule execution, independent verification,
replay, trace-derived graph provenance and bounded simulation-only description.
Full Draft exposure, aggregate representation and logical-time assumptions are
part of the result.

It does not establish source reliability, bank-level or depositor behavior,
historical fit, calibration, held-out performance, contagion causality, rescue or
policy effects, scientific validity or universal generality. No external
research, Reference or model decision was used.
