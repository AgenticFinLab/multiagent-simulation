# Samsung Galaxy Note7 Battery Recall

`H2EPR-0481` is a complete current Rule event.

## Event assets

- `source-profile.json` admits the three dataset inputs and fixes the claim boundary.
- `package-assembly.json` binds all semantic and backend releases.
- `package/` is the compiled backend-neutral runtime input with its Rule binding.

The source roster contains nine Draft participants. Actorization yields six
named Agents and two aggregate Population Models; test-unit owners remain
scenario state because the exposed process assigns them no autonomous
decision.

## Backend status

- Rule: implemented and admitted.
- LLM: planned and fails closed.
- RuleLLM: planned and fails closed.

## Current result

The Rule run closes 19 logical coordinates and 729 trace records. Two
canonical materializations are byte identical; the generated-identity probe
preserves trajectory semantics; replay reproduces the terminal state; all 729
trace records appear in a 772-node, 1,872-edge Generated EPG; and no transport
intent remains unresolved.

See the [compact release](../../releases/samsung_note7_battery_recall/rule/) and
[simulation reading](../../reports/samsung_note7_battery_recall/rule/simulation-reading.md).

## Claim boundary

Full Draft exposure makes this a dataset-conditioned construction baseline.
It supports engineering and method verification only, not historical fit,
parameter calibration, recall effectiveness, held-out performance, causality,
scientific validity, or universal generality.
