# SingHealth Data Breach

`H2EPR-0616` is a complete current Rule event.

## Event assets

- `source-profile.json` admits the three dataset inputs and fixes the claim boundary.
- `package-assembly.json` binds all semantic and backend releases.
- `package/` is the compiled backend-neutral runtime input with its Rule binding.

The source roster contains nine Draft participants. Actorization yields eight
named Agents; the affected-patient cohort remains scenario state because the
exposed process assigns it no autonomous decision.

## Backend status

- Rule: implemented and admitted.
- LLM: planned and fails closed.
- RuleLLM: planned and fails closed.

## Current result

The Rule run closes 11 logical coordinates and 438 trace records. Two
canonical materializations are byte identical; the generated-identity probe
preserves trajectory semantics; replay reproduces the terminal state; all 438
trace records appear in a 466-node, 1,131-edge Generated EPG; and no transport
intent remains unresolved.

See the [compact release](../../releases/singhealth_data_breach/rule/) and
[simulation reading](../../reports/singhealth_data_breach/rule/simulation-reading.md).

## Claim boundary

Full Draft exposure makes this a dataset-conditioned construction baseline.
It supports engineering and method verification only, not historical fit,
parameter calibration, held-out performance, causality, scientific validity,
or universal generality.
