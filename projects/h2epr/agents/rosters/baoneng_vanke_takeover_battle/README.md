# Baoneng–Vanke Takeover Battle roster

The source roster accounts for all eight IDs and 33 appearances across four
stages and ten episodes. The actor map retains eight named decision interfaces.
No source participant is dropped and no Population Model is required by this
roster. Frozen-only investors, analysts, exchanges and election voters remain
explicitly outside the executable choice boundary described in the event entry.

`roster.json` owns source occurrence coverage; `actor-map.json` owns representation
choices and losses. The issuer/board composite P_2 is distinct from Wang and Yu;
passive later holdings and nomination do not create extra autonomous actions.
Corrupt relation/transaction endpoints are not executable authority.

The manifest and SHA256SUMS seal this current release. Changed representation
requires new parent identities and all dependent admission evidence.
