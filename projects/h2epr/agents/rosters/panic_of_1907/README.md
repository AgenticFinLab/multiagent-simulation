# Panic of 1907 roster

The roster accounts for all 16 source IDs and 33 appearances across three
stages and ten episodes. Eleven runtime participants retain bounded choices;
five remain explicit context. P_8 is absent from the Draft and is not inferred.

`roster.json` owns exact Draft accounting. `actor-map.json` owns representation
and loss decisions. Malformed relation/transaction endpoints, passive outcomes
and duplicated evidence do not silently create authority. The manifest and
checksums seal the single current release.
