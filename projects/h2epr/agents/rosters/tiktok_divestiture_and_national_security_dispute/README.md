# TikTok dispute roster

The roster accounts for all ten source IDs and 26 appearances across four
stages and ten episodes. Nine Agent/representation-gate parents and one
Population Model retain bounded choices; there is no numeric source-ID gap.

`roster.json` owns exact Draft accounting. `actor-map.json` owns representation
and loss decisions. Passive ByteDance receipt rows and misdirected Draft
relations do not create authority. The manifest and checksums seal the current
release.
