# SingHealth Data Breach participant interfaces

Eight Agent Definitions project to eight actors, five observation classes, 17
event intents plus `no_op`, and two lifecycles. The participant semantic index
seals each parent and every Draft appearance. P_4 remains accounted context.

Attacker representation, data owner, system operator, leadership, ministry,
committee, regulator and attribution publisher retain separate authority.
Messages carry qualified records and never grant state writes. Actual delivery
and runtime memory govern information; drift routes to its owning parent before
package compilation.
