# Panic of 1907 interface closure

## Scope and parent closure

The complete 16-ID, 33-appearance source roster maps to eight Agent parents,
three Population Models and five initial-context entities. The source sequence
gap P_8 is explicit. The participant semantic index seals every active human
parent and every corresponding Draft appearance; no missing clearing agent is
silently synthesized.

## Source-action crosswalk

The Draft contains 34 actor-local rows. Twenty-three current intents retain,
split or qualify supported participant meanings. Stock-target status, indirect
causation, forced resignation, being run on, assistance receipt, exchange stress,
system recovery and Federal Reserve establishment remain context or results.

Malformed E2/E4/E6/E8 relationship and transaction endpoints confer no
permission. Withdrawal, request, disposition, support, coordination, aggregate
flow and legislation records each have one exact owner and target.

## Observation, effect, and lifecycle closure

All eleven actors receive four maintained observation classes and two maintained
lifecycles. Twenty-three fields and handlers are one-to-one. Actual delivery and
persistent runtime memory precede guarded decisions; accepted actions alone
change state. Messages neither move money nor write recipient fields.

## Review cases and failure routing

Missing requests or late recommendations may produce open expected fields with
a valid terminal run. Foreign actor, wrong target, invalid parameter or
undeclared message produces typed rejection and no delta. Pending transport must
resolve by the terminal barrier. Admission fails on schema, path, identity,
provenance, source or semantic-parent drift. Runtime failure preserves sealed-
prefix custody; replay, graph or rematerialization mismatch rejects publication.

## Disposition

Semantic closure is accepted for compiler/runtime verification with the noisy-
input, duplicate-source, P_8-gap, aggregate-population, no-money and long-horizon
limitations above. Fresh execution and independent publication remain separate
evidence owners.
