# Shared changes required — CarryTradeUnwind polish run (2026-07-12)

This file describes changes that lie **outside** the scenario folder
`examples/CarryTradeUnwind/` (and `configs/CarryTradeUnwind/`) and MUST
be merged by the main session because the concurrent polish worktree
cannot safely commit shared artefacts alongside sibling-scenario polish
runs.

## 1. `examples/AGENT_POOL/agent_images/icons/*.png` — four new PNG files

During the CarryTradeUnwind Step 2 icon-resolution audit, four of the
five CarryTradeUnwind pool profiles had **no Icon row** in their `Design
Provenance and Versioning` table and **no matching PNG** under
`examples/AGENT_POOL/agent_images/icons/`. Only `noise-trader.md` was
already fully icon-registered.

The four missing PNGs have been generated in place by this polish run
(via `ImageGen` following the `agent-icon-generation-skill.md` prompt
template — 512×512, circular flat-badge, motif + Chinese-label
composition). The files are already present on disk at the canonical
paths below and are untracked; the main session should `git add` them:

- `examples/AGENT_POOL/agent_images/icons/finance-carry-trader.png`
  (motif: currency-arrow rising then plunging; label: 套息型投资者)
- `examples/AGENT_POOL/agent_images/icons/finance-leveraged-carry-fund.png`
  (motif: lever/scale + stop-loss line + margin-call bell; label: 杠杆套息基金型投资者)
- `examples/AGENT_POOL/agent_images/icons/finance-funding-currency-buyer.png`
  (motif: shield + JPY/CHF glyph + inward safe-haven arrows + anchor;
  label: 避险货币买入型投资者)
- `examples/AGENT_POOL/agent_images/icons/finance-hedged-carry-trader.png`
  (motif: carry-arrow + protective umbrella + volatility waveform;
  label: 对冲型套息投资者)

## 2. `examples/AGENT_POOL/finance/{carry-trader,leveraged-carry-fund,funding-currency-buyer,hedged-carry-trader}.md` — append `Icon` rows

Each of the four `Design Provenance and Versioning` tables in the four
CarryTradeUnwind pool profiles currently ends with:

```
| Version | 1.0.0 |
| Change log | Initial CarryTradeUnwind ... |
| Status | draft |
```

Please insert one `Icon` row per profile between `Version` and `Change
log`, and append one `1.0.1` change-log entry recording the icon repair
under the `agent-icon-generation-skill.md` protocol. The four inserts
are shown below verbatim (formatted to align with the existing table
padding in each file).

**`carry-trader.md`** (currently ends at line 208):

```
| Icon | ![](../agent_images/icons/finance-carry-trader.png) |
| Change log | Initial CarryTradeUnwind AGENT_POOL design<br>1.0.1 - Added AGENT_POOL Icon row via polish-simulation-pipeline Step 2 icon-repair (icon PNG generated by ImageGen in the same session; profile row and design.md mapping were the missing links). |
```

**`leveraged-carry-fund.md`** (currently ends at line 203):

```
| Icon | ![](../agent_images/icons/finance-leveraged-carry-fund.png) |
| Change log | Initial CarryTradeUnwind fork from leverage/funding-stress family<br>1.0.1 - Added AGENT_POOL Icon row via polish-simulation-pipeline Step 2 icon-repair (icon PNG generated by ImageGen in the same session; profile row and design.md mapping were the missing links). |
```

**`funding-currency-buyer.md`** (currently ends at line 202):

```
| Icon | ![](../agent_images/icons/finance-funding-currency-buyer.png) |
| Change log | Initial CarryTradeUnwind AGENT_POOL design<br>1.0.1 - Added AGENT_POOL Icon row via polish-simulation-pipeline Step 2 icon-repair (icon PNG generated by ImageGen in the same session; profile row and design.md mapping were the missing links). |
```

**`hedged-carry-trader.md`** (currently ends at line 209):

```
| Icon | ![](../agent_images/icons/finance-hedged-carry-trader.png) |
| Change log | Initial CarryTradeUnwind fork from volatility-managed hedging family<br>1.0.1 - Added AGENT_POOL Icon row via polish-simulation-pipeline Step 2 icon-repair (icon PNG generated by ImageGen in the same session; profile row and design.md mapping were the missing links). |
```

Also bump `Version` from `1.0.0` to `1.0.1` in each of the four
profiles.

## 3. `examples/AGENT_POOL/agent_images/design.md` — append mapping rows

The mapping table currently lists 32 rows (through
`finance/value-contrarian.md`, appended by the AsianFinancialCrisis
polish run). Please append the following four rows (#33–#36) to the
mapping table, bump any `Icons are N total` counter accordingly, and
add a new dated note:

| #  | Agent                                | Icon                                    | Display Name             | Match Reason                                                                                                     |
|----|--------------------------------------|-----------------------------------------|--------------------------|------------------------------------------------------------------------------------------------------------------|
| 33 | `finance/carry-trader.md`            | `finance-carry-trader.png`              | 套息型投资者             | Leveraged FX carry accumulator / crash-risk exposure → currency-arrow motif with up-stairs / down-elevator tail  |
| 34 | `finance/leveraged-carry-fund.md`    | `finance-leveraged-carry-fund.png`      | 杠杆套息基金型投资者     | Stop-loss-triggered forced-liquidation hedge fund → lever + stop-loss line + margin-call bell motif              |
| 35 | `finance/funding-currency-buyer.md`  | `finance-funding-currency-buyer.png`    | 避险货币买入型投资者     | Safe-haven / repatriation JPY-CHF buyer → shield + inward arrows + anchor motif                                  |
| 36 | `finance/hedged-carry-trader.md`     | `finance-hedged-carry-trader.png`       | 对冲型套息投资者         | Volatility-managed macro fund with options overlay → carry-arrow + umbrella + volatility waveform motif          |

Suggested `Notes` addition (append after the 2026-07-11
AsianFinancialCrisis note):

- 2026-07-12: Mapping rows #33–#36 added for the CarryTradeUnwind
  scenario archetypes (carry-trader, leveraged-carry-fund,
  funding-currency-buyer, hedged-carry-trader). Icons were generated
  in-session via ImageGen following
  `agent-icon-generation-skill.md`; the mapping-row gap and the
  profile-`Icon`-row gap were closed together as part of the
  polish-simulation-pipeline Step 2 icon-resolution sub-gate.

Schema recap (as required by the polish pipeline concurrency contract):

| agent_stem              | icon_filename                          | display_name          | match_reason                                                                                                    |
|-------------------------|----------------------------------------|-----------------------|-----------------------------------------------------------------------------------------------------------------|
| carry-trader            | finance-carry-trader.png               | 套息型投资者          | Leveraged FX carry accumulator / crash-risk exposure → currency-arrow motif with up-stairs / down-elevator tail |
| leveraged-carry-fund    | finance-leveraged-carry-fund.png       | 杠杆套息基金型投资者  | Stop-loss-triggered forced-liquidation hedge fund → lever + stop-loss line + margin-call bell motif             |
| funding-currency-buyer  | finance-funding-currency-buyer.png     | 避险货币买入型投资者  | Safe-haven / repatriation JPY-CHF buyer → shield + inward arrows + anchor motif                                 |
| hedged-carry-trader     | finance-hedged-carry-trader.png        | 对冲型套息投资者      | Volatility-managed macro fund with options overlay → carry-arrow + umbrella + volatility waveform motif         |

## 4. Nothing else required

No changes to `masim/evaluation/finance/*.py` or any other shared module
are needed by this polish run — `Rule/analysis.py` already computes its
metrics via local scenario-specific helpers (`_compute_max_drawdown`,
`_compute_unwind_velocity`, `_compute_agent_volume_share`,
`_compute_recovery_ratio`, `_compare_exit_rounds`), all documented in
`analysis-bases.md §2` under scenario-specific naming. The local
`_batch_to_rounds` data loader in `Rule/analysis.py` is retained under
Analysis Migration Rule step 4 as scenario-specific FX-payload
round-extraction logic that no other scenario currently reuses. All
config `# Source:` comment additions and per-variant explain.md /
analysis.md doc updates are scenario-local and are handled by the
polish worktree directly.
