"""AssetBubbleRag — RAG-augmented hybrid Rule+LLM asset bubble simulation.

Three-way comparison:
    AssetBubble        — pure rule-based
    AssetBubbleRuleLLM — rule-embedded LLM (persona + quantitative rules in prompt)
    AssetBubbleRag     — rule-embedded LLM + personal RAG library (retrieved context per decision)
"""
